<html>
  <body>
    <div class="container justify-content-center text-center">
      <div class="col-lg-12">
        <h1>Bienvenue sur la page de l'utilisateur <%= current_user.email %> </h1>
	    <% @user.events.each do |event| %>
		  <div class="card">
			<div class="card-body">
			  <h3 class="card-title"><%=link_to event.title, event_path(event.id) %> </h3>			          	
			</div>	
	      </div> 
	    <% end %>
        <p>
          <%= link_to 'Changer ses informations de profil', edit_user_registration_path %>
        </p>
      </div>
    </div>
  </body>
</html>